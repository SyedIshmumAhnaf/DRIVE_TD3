gamma: 0.99          # Discount factor
tau: 0.005           # Target network update rate
actor_lr: 1e-4       # Actor learning rate
critic_lr: 1e-3      # Critic learning rate
policy_noise: 0.2    # Target action noise
noise_clip: 0.5      # Noise clipping range
policy_freq: 2       # Delayed policy update frequency
buffer_size: 1000000  # Default size for TD3
exploration_noise: 0.1  # Standard exploration noise for TD3
replay_size: 1000000  # Needed for GPU replay buffer
saliency: "MLNet"  # ✅ Or "TASED-Net" if using that model
input_shape: [480, 640]
len_clip: 32
image_shape: [330, 792]
frame_interval: 5
